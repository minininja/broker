package org.dorkmaster.comms;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends ChannelResourceTest {

    // Execute the same tests but in native mode.
}